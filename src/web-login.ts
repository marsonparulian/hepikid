import { Page } from "puppeteer"

// This file contains helpers to automate puppeteer's page login to Shoppee seller center

const login = async (page: Page): Promise<any> => {
    // TODO Login if needed
}

export default login;
